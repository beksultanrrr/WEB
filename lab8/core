from django.contrib import admin

# Register your models here.
from core.models import Product

admin.site.register(Product)from django.apps import AppConfig


class CoreConfig(AppConfig):
    name = 'core'
